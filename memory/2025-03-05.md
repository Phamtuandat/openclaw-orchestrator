# 2025-03-05 - OpenClaw Orchestrator Fixes

## Goals Completed
1. ✅ Dashboard port: 3001 (was 3000 in docs)
2. ✅ Orchestrator-main no fallback
3. ✅ Real conflict serialization with file resolution
4. ✅ Compatibility maintained
5. ✅ Docs updated

## Changes Made
- dashboard.ts: PORT = 3001 (already set)
- README-DASHBOARD.md: Updated port references to 3001
- core/orchestrator.ts:
  - getFallbackModel: return null for orchestrator-main
  - collectFileIntents: resolve glob patterns using file index
  - registerFileIntent: track per-file locks
  - waitIfConflicted: check file-level conflicts

## Testing
- TypeScript: npm run build (to be done)
- Workflow simulation: to be tested

## Notes
- Port conflict resolution: dashboard uses 3001, if conflict change to 4000
- Conflict serialization now uses actual file paths, not just patterns

---
## 2025-03-05 14:00+ — WebSocket Dispatcher & Real Agent Integration

### Goal
Implement WebSocket-based real agent dispatcher to replace HTTP REST (which returned 404).

### Implementation
- Created `OpenClawWsClient` in `core/openclaw_client.ts`
  - Connects to `ws://127.0.0.1:18789` with token auth
  - Implements JSON-RPC 2.0 style request/response
  - Methods attempted: `sessions.spawn`, `spawnSession`, `session.spawn`, `createSession`, `spawn`, `run`
  - `waitForSession` polls `sessions.get`/`getSession`/`session.get`/`status`
  - Auto-reconnect on connection close with exponential backoff
- Dispatcher selection logic:
  - If URL starts with `ws://` or `wss://` → use WS client
  - Otherwise fallback to HTTP client (if implemented)
- Added `ws` npm dependency

### Test Results: USE_REAL_AGENT=true
- Workflow: `crash_hunter`
- Outcome: **FAILED** – All stages returned "All WS spawn methods failed"
- Observations from logs:
  - WS connection established successfully with token
  - After sending request, connection closed by gateway
  - Reconnect loop ensued
  - No valid response received for any spawn method
- Conclusion: Gateway is rejecting WS messages. Protocol mismatch or command blocked.

### Next Steps (Incomplete)
- Need to identify exact WS message envelope expected by gateway
- Investigate gateway command router (denyCommands, policy)
- Enable gateway debug logs to see why connections close
- Determine correct method name and request format
- Possibly need to use `sessions_spawn` tool call instead of direct WS

### Files Modified
- `core/openclaw_client.ts` – complete rewrite with WS client
- `package.json` – added `ws` dependency
- `core/agent_dispatcher.ts` – already had dispatcher selection

### Status
- Conflict detection verified ✅
- Real agent integration **blocked** by gateway protocol unknown
- Code ready to work once protocol is identified

---
## 2025-03-05 14:20+ — Gateway Debug Investigation

### Actions
- Enabled debug logging via config patch
- Restarted gateway with `log.level: debug`
- Attempted to capture WS handshake and message format
- Still unable to see detailed WS messages in gateway logs (log rotation?)

### Findings
- Gateway accepts WS connection and authenticates
- Sending `sessions.spawn` causes connection termination
- This suggests:
  - Method not recognized/allowed
  - Missing required fields in request
  - Command blocked by `gateway.nodes.denyCommands` (list does not include spawn-related)
  - Need to inspect gateway source to find command router

### To-Do
- Find gateway command handling code (likely in `/opt/homebrew/lib/node_modules/openclaw/dist/`)
- Identify exact method names and request envelope
- Test single valid request and capture response/error
