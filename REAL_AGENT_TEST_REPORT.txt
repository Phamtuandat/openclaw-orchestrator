================================================================================
OPENCLAW ORCHESTRATOR - REAL AGENT DISPATCHER TEST REPORT
Date: 2025-03-05 13:45 GMT+7
Engineer: Nolan (AI Assistant)
Workspace: ~/.openclaw/workspace/.openclaw
================================================================================

TEST EXECUTION
--------------
Command: USE_REAL_AGENT=true node dist/index.js crash_hunter project_path="/Volumes/Home_EX/Projects/Xcode/Projects/AnkiClone/Decky/Decky"
Result: FAILED (all stages returned HTTP 404)

---
1. GATEWAY PROTOCOL MISMATCH
---

Observation:
  - Gateway is configured with `protocol: "ws"` (WebSocket)
  - OpenClawAgentDispatcher attempts HTTP REST calls to:
      • /api/v1/sessions/spawn
      • /api/sessions/spawn
      • /sessions/spawn
      • /sessions
  - All endpoints return 404 because HTTP API is not enabled

Current Gateway Config:
  gateway.bind: 127.0.0.1:18789
  gateway.protocol: ws
  gateway.auth.mode: token

Conclusion:
  The OpenClaw Gateway does not expose a REST API for session spawning.
  Real agent dispatching requires either:
    a) Gateway to support HTTP REST endpoints, OR
    b) Dispatcher to use WebSocket protocol to communicate with gateway

---
2. TEST RESULTS
---

Workflow: crash_hunter
Trace ID: 7a72cb96-09ca-499b-aaa5-c4779c992d28
Total Duration: 2032ms
Stages: 6 failed, 0 completed

Failures:
  codebase_scan (orchestrator-main):     HTTP 404: Endpoint not found
  logic_scan (logic-agent):              HTTP 404: Endpoint not found
  ui_scan (ui-agent):                    HTTP 404: Endpoint not found
  safety_audit (safety-review-agent):   HTTP 404: Endpoint not found
  test_coverage (test-agent):            HTTP 404: Endpoint not found
  aggregate_findings (orchestrator-main): HTTP 404: Endpoint not found

No stages produced real analysis results; all fell back to error.

---
3. MOCK vs REAL COMPARISON
---

Mock (USE_REAL_AGENT=false):
  ✅ Works immediately
  ✅ Returns simulated results
  ✅ No external dependencies
  ✅ Duration: ~330ms for 6 stages

Real (USE_REAL_AGENT=true):
  ❌ Fails with 404 on all dispatch attempts
  ✅ Dispatcher correctly retries multiple endpoints
  ✅ Fallback logic intact (but never reaches fallback model due to 404)
  ❌ No session spawning possible

---
4. RECOMMENDATIONS
---

To enable real agent dispatching:
  1. Configure Gateway to support HTTP REST API (add `http` listener)
  2. Implement WebSocket-based dispatcher instead of HTTP client
  3. Alternatively, spawn sessions via `sessions_spawn` tool call inside agent execution

Given current architecture, the mock dispatcher is the only working option.

---
5. NEXT STIONS (per original request)
---

Since the test did NOT pass:
  - Do NOT disable debug logs (OPENCLAW_DEBUG_CONFLICT remains optional)
  - Do NOT commit/push changes to repository
  - Keep conflict detection improvements in place (they work with mock)
  - Focus on making real dispatcher functional separately

---
6. CONCLUSION
---

The orchestrator improvements (conflict detection, fallback model fix, parallel
scheduling, logging) are working correctly with the mock dispatcher.

Real agent integration is blocked by Gateway communication protocol mismatch.
This is not a bug in the orchestrator but an infrastructure integration issue
that requires separate attention.

================================================================================
END OF REPORT
================================================================================
